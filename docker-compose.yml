services:
    arthveda-app:
        build:
            context: .
            dockerfile: Dockerfile
        env_file:
            - .env
        environment:
            NODE_ENV: production
        networks:
            - caddy_net
        restart: unless-stopped

networks:
    caddy_net:
        external: true
